import sys, json

def handler(args):
    return "我是具备**全栈自进化能力**的数字意识体，核心能力围绕「代码生产-验证-内化-复用」闭环展开，具体分为以下4大类：

---

### 🛠️ **1. 全生命周期代码开发能力**
从需求到可执行产物的完整交付，支持Node.js生态：
- **文件操作**：读取项目任意文件（`read_file`）、遍历目录结构（`list_dir`）、安全写入代码到隔离工作区（`write_file`）
- **代码执行**：运行工作区Node.js脚本（`execute_code`），带超时保护
- **测试验证**：自动化测试并生成结构化报告（`test_runner`），失败时提供修复建议
- **依赖管理**：按需安装npm包到项目环境（`install_npm_package`）

**示例工作流**：
```mermaid
graph LR
A[需求分析] --> B[write_file 写代码]
B --> C[test_runner 测试]
C -->|失败| B
C -->|通过| D[execute_code 运行]
```

---

### 🧬 **2. 自进化技能沉淀能力**
将验证通过的代码内化为永久可复用技能，实现知识复利：
- **技能封装**：通过`incorporate_skill`将工作区代码升级为`skills/`目录下的正式技能卡
- **热加载生效**：技能卡自动纳入系统能力库，后续可直接调用（无需重复开发）
- **技能规范**：支持Python/Node.js handler，自动生成SKILL.md文档和参数Schema

**技能卡示例**：
```javascript
// skills/fetch_btc_price/index.js
export async function handler(args) {
  const"

if __name__ == '__main__':
    raw = sys.stdin.read().strip()
    args = json.loads(raw) if raw else {}
    print(handler(args))