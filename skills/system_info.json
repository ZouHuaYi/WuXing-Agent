{
  "name": "get_system_info",
  "description": "获取当前运行环境的系统信息：Node.js 版本、平台、内存用量、CPU 数量、进程运行时长。适合排查环境问题或了解系统资源时调用。",
  "parameters": {
    "type": "object",
    "properties": {
      "detail": {
        "type": "string",
        "description": "查询维度：all（全部信息）/ memory（内存）/ platform（平台）/ uptime（运行时长）。默认 all。"
      }
    },
    "required": []
  }
}
