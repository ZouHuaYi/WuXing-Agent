{
  "name": "get_datetime",
  "description": "获取当前系统时间。可以格式化为完整时间、仅日期或仅时间。适合在回答中需要显示当前时间时调用。",
  "parameters": {
    "type": "object",
    "properties": {
      "format": {
        "type": "string",
        "description": "输出格式：full（完整日期时间）/ date（仅日期）/ time（仅时间）/ iso（ISO 8601）。默认 full。"
      },
      "timezone": {
        "type": "string",
        "description": "IANA 时区名称，如 'Asia/Shanghai'。默认使用系统时区。"
      }
    },
    "required": []
  }
}
